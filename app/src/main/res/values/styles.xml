<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/color_light</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

</resources>
